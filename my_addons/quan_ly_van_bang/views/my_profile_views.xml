<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_res_users_form_inherit" model="ir.ui.view">
        <field name="name">res.users.form.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_simple_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="replace">
                <sheet>
                    <field name="id" invisible="1"/>
                    <div class="alert alert-info text-center mb-3" invisible="id &gt; 0" role="alert">
                        You are inviting a new user.
                    </div>
                    <field name="avatar_128" invisible="1"/>
                    <field name="image_1920" widget='image' class="oe_avatar" options='{"zoom": true, "preview_image": "avatar_128"}'/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="VD: Tran Duc Thang" required="1"/>
                        </h1>
                        <field name="email" invisible="1"/>
                        <label for="login" string="Email Address"/>
                        <h2>
                            <field name="login" placeholder="e.g. email@yourcompany.com"/>
                        </h2>
                    </div>
                    <group name="phone_numbers">
                        <field name="company_id" context="{'user_preference': 0}" groups="base.group_multi_company"/>
                        <label for="groups_id" string="Access Rights" invisible="id &gt; 0" groups="base.group_no_one"/>
                        <div invisible="id &gt; 0" groups="base.group_no_one">
                            <field name="groups_id" readonly="1" widget="many2many_tags" options="{'color_field': 'color'}" style="display: inline;"/>
 You will be able to define additional access rights by editing the newly created user under the Settings / Users menu.
                        </div>
                        <field name="phone" widget="phone"/>
                        <field name="mobile" widget="phone"/>
                    </group>
                    <group name="Personal Detail">
                        <field name="date_of_birth"/>
                        <field name="national_id"/>
                        <field name="address"/>
                    </group>
                    <notebook>
                        <page string="Certificates">
                            <field name="certificate_ids" widget="one2many_list">
                                <tree create="false">
                                    <field name="certificate_type_id" string="Certificate Type"/>
                                    <field name="certificate_code" string="Certificate Code"/>
                                    <field name="status" string="Status"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </xpath>
        </field>
    </record>

    <record id="action_open_current_user_form" model="ir.actions.server">
        <field name="name">Open My User Form</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="binding_model_id" ref="base.model_res_users"/>
        <field name="state">code</field>
        <field name="code">
            action = {
                "type": "ir.actions.act_window",
                "res_model": "res.users",
                "res_id": user.id,
                "view_mode": "form",
                "target": "current",
            }
        </field>
    </record>

</odoo>